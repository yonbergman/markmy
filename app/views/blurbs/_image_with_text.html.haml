.blurb
  .bg
    .img
      = image_tag(blurb.image_url)
  .content
    .text
      %span= blurb.text
      .timeago{'data-livestamp' => blurb.time.to_i}
    .by
      %i.fa.fa-user
      %span= t('blurb.who', user: blurb.username)
